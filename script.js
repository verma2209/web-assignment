// "Shop Now in navbar" button click alert
document.querySelector(".shop-now").addEventListener("click", function () {
  alert("Redirecting to Shop Page!");
});
// "Shop Now in middle" button click alert
document.querySelector(".shop-now-hero").addEventListener("click", function () {
  alert("Redirecting to Shop Page!");
});

// Function to show alert when an item is added to cart
function addToCart() {
  alert("Item added to cart!");
}
